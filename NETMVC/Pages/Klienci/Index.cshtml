@page
@model NETMVC.Pages_Klienci.IndexModel

@{
    ViewData["Title"] = "Index";
}

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Klient[0].name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Klient[0].surname)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Klient[0].pesel)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Klient[0].birthyear)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Klient[0].płeć)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Klient) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.surname)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.pesel)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.birthyear)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.płeć)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>